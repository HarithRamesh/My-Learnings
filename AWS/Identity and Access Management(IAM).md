AWS IAM (Identity and Access Management) — Notes

1. What is IAM?

IAM is a global AWS service used to manage authentication (who can log in) and authorization (what they can access).

IAM enables you to manage:

Users

Groups

Roles

Policies

IAM is global (not tied to any region).
